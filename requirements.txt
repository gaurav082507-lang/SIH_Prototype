# --- API server ---
fastapi
uvicorn[standard]

# --- Existing pipeline (LangGraph / LangChain) ---
langgraph
langchain
langchain-core
langchain-google-genai
pydantic
python-dotenv

requests
pandas
numpy

openmeteo-requests
requests-cache
retry-requests

# global-land-mask
#   No longer needed. It answered "is this point on land" by
#   decompressing a global boolean raster into RAM — measured at
#   +341 MB on first call, which OOM-killed the 512 MB instance.
#   coastline.py replaces it at ~0.7 MB. Re-enable this line only
#   if you set COASTAL_CHECK_MODE=landmask.
